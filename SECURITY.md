# Security

This tool runs local commands (`xcur2png`, `xcursorgen`, ImageMagick) on paths you choose.  
It does not download anything and does not need network access.

Only run it on cursor themes and files you trust.

If you find a security issue that could expose user files or run unintended commands, contact the maintainer privately instead of opening a public issue.
